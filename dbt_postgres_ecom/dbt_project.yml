name: 'dbt_postgres_ecom'
version: '1.0.0'
config-version: 2
profile: 'dbt_postgres_ecom'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  dbt_postgres_ecom:
    staging:
      +materialized: view
    marts:
      +materialized: table
